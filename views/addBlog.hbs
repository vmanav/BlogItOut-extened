<!-- visit : https://bootstrapious.com/p/bootstrap-sidebar -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>New Blog</title>

    <!-- bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>

    <!-- font awesom 5 kit code -->
    <script src="https://kit.fontawesome.com/e00a1e7cf5.js" crossorigin="anonymous"></script>

    <!-- my script for dashboard side-navbar -->
    <script src="../scripts/dashnav.js"></script>

    <style>
        #main {
            background-color: #F7F4F3;
            min-height: 100vh;
            word-wrap: break-word;
        }

        /* pageContent ki css */
        .card {
            /* shadow  */
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            transition: 0.3s;
        }

        .card:hover {
            box-shadow: 0 12px 20px 0 rgba(0, 0, 0, 0.4);
        }

        #pageContent {
            min-height: 100vh;
            display: block;
        }

        .blackCont {
            /* word-wrap: break-word; */
            background-image: radial-gradient(circle farthest-corner at 10% 20%, rgba(32, 45, 58, 1) 0%, rgba(90, 92, 106, 1) 81.3%);
            border-radius: 0% 100% 100% 0% / 0% 50% 50% 100%;
        }

        .offWhiteContainer {
            background-color: #F7F4F3;
            border-radius: 100% 0% 0% 100% / 50% 100% 0% 50%
        }
    </style>

    {{> dashnavStyling}}



</head>

<body>

    <div class="d-flex" id="main">

        <!-- sidebar here -->
        {{> dashnavPartial}}

        <div id="pageContent" class="p-4">

            <!-- Title -->
            <div class="card p-2 h5" style="display: inline;">
                New Blog
            </div>
            <hr>

            <form action="/addBlog" method="POST">
                <div class="form-group">
                    <label>Title</label>
                    <input type="text" class="form-control" name="title" placeholder="Enter blog title">
                    <small class="form-text text-muted">This will be the title of your blog.</small>
                </div>

                <div class="form-group">
                    <label>Body</label>
                    <textarea type="text" class="form-control" name="body" placeholder="Enter blog body"
                        rows="6"></textarea>
                    <small class="form-text text-muted">This will be the content of your blog.</small>
                </div>

                <div class="form-group">
                    <label>Author</label>
                    <input type="text" class="form-control" name="author" value='{{firstName}} {{lastName}}' disabled>
                    <small class="form-text text-muted">This will be the Author Name for your blog.</small>
                </div>

                <div class="form-group">
                    <label>Tags</label>
                    <input type="text" class="form-control" name="tags"
                        placeholder="Enter Tags According to Title and Body">
                    <small class="form-text text-muted">These will reflect the keypoints of your post.</small>
                </div>

                <button type="submit" class="btn btn-dark">Submit</button>

            </form>

        </div>

    </div>

</body>

</html>